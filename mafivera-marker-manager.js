/* MAFIVERA V2 — legacy marker engine retired.
   The main game runtime owns the map/grid markers.
   mafivera-dealer-v2.js owns the dealer marker.
   This compatibility shim intentionally renders nothing, preventing duplicate
   marker engines and keeping older launchers compatible. */
(()=>{'use strict';window.__mtrwLegacyMarkerEngineRetired=true;})();
