/* MAFIVERA – territory controls: demolish buildings / release territories + live modules */
(()=>{'use strict';
  let busy=false;
  const toast=(text,error=false)=>{const x=document.getElementById('toast');if(!x)return;x.textContent=text;x.className='toast show '+(error?'error':'');clearTimeout(toast.t);toast.t=setTimeout(()=>x.className='toast',2800)};
  const db=()=>window.db;
  async function rpc(name,args){const d=db();if(!d)throw Error('Datenbank noch nicht bereit.');const r=await d.rpc(name,args);if(r.error)throw r.error;return r.data}
  function zoneFromDrawer(){const body=document.getElementById('drawerBody');if(!body)return null;const el=body.querySelector('[data-action="upgrade"],[data-action="build-menu"],[data-action="station"]');return (el?.dataset.zone||'').split('|')[0]||null}
  async function demolish(zone){if(busy)return;busy=true;try{if(!confirm('Gebäude wirklich abreißen? Du erhältst 50 % der bisher investierten Baukosten zurück.'))return;const r=await rpc('mafivera_demolish',{p_zone_key:zone});toast(`Gebäude abgerissen. Erstattung: ${Number(r?.refund||0).toLocaleString('de-DE')} $`);setTimeout(()=>location.reload(),350)}catch(e){toast(e.message||'Gebäude konnte nicht abgerissen werden.',true)}finally{busy=false}}
  async function leave(zone){if(busy)return;busy=true;try{if(!confirm('Gebiet wirklich verlassen? Das Gebiet wird sofort frei. Alle Gebäude gehen verloren. Stationierte Schläger werden in deinen Bestand zurückgeführt.'))return;const r=await rpc('mafivera_leave_territory',{p_zone_key:zone});const n=Number(r?.returned_hitmen||0);toast(`Gebiet freigegeben.${n?` ${n} Schläger zurückerhalten.`:''}`);setTimeout(()=>location.reload(),350)}catch(e){toast(e.message||'Gebiet konnte nicht freigegeben werden.',true)}finally{busy=false}}
  function enhance(){const body=document.getElementById('drawerBody');if(!body||document.getElementById('mtrwTerritoryControls'))return;const zone=zoneFromDrawer();if(!zone)return;const ownBuilding=body.querySelector('.building-card');const wrap=document.createElement('div');wrap.id='mtrwTerritoryControls';wrap.className='mtrw-territory-controls';if(ownBuilding){const b=document.createElement('button');b.className='action danger';b.type='button';b.textContent='🧱 Gebäude abreißen · 50 % Erstattung';b.onclick=()=>demolish(zone);wrap.appendChild(b)}const leaveBtn=document.createElement('button');leaveBtn.className='action danger';leaveBtn.type='button';leaveBtn.textContent='🚪 Gebiet verlassen · Gebiet freigeben';leaveBtn.onclick=()=>leave(zone);wrap.appendChild(leaveBtn);body.appendChild(wrap)}
  const observer=new MutationObserver(()=>setTimeout(enhance,0));
  function boot(){const body=document.getElementById('drawerBody');if(!body){setTimeout(boot,500);return}observer.observe(body,{childList:true,subtree:true});enhance()}
  function loadScript(src,key){if(document.querySelector(`script[data-mtrw-live="${key}"]`))return;const x=document.createElement('script');x.src=src;x.dataset.mtrwLive=key;x.async=false;document.body.appendChild(x)}
  function loadLiveModules(){const v='20260917-clean1';loadScript(`./mafivera-dealer-ui.js?v=${v}`,'dealer-ui');loadScript(`./mafivera-buildings.js?v=${v}`,'buildings');loadScript(`./mafivera-admin.js?v=${v}`,'admin')}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{boot();loadLiveModules()});else{boot();loadLiveModules()}
})();